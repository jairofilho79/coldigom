{
  "devDependencies": {
    "wrangler": "^4.112.0"
  },
  "scripts": {
    "dev:api": "cd api && npm run dev",
    "dev:web": "cd web && npm run dev",
    "build:api": "cd api && npm run build",
    "build:web": "cd web && npm run build",
    "typecheck:api": "cd api && npx tsc --noEmit",
    "typecheck:web": "cd web && npx tsc -b --force",
    "typecheck": "npm run typecheck:api && npm run typecheck:web",
    "lint": "npm run lint:api && npm run lint:web",
    "test:api": "cd api && npx vitest run --pool=threads",
    "test:web": "cd web && npx vitest run --pool=threads",
    "test": "npm run test:api && npm run test:web",
    "coverage:api": "cd api && npx vitest run --pool=threads --coverage",
    "coverage:web": "cd web && npx vitest run --pool=threads --coverage",
    "coverage": "npm run coverage:api && npm run coverage:web",
    "verify": "npm run typecheck && npm run coverage",
    "verify:strict": "npm run typecheck && npm run lint && npm run coverage",
    "lint:api": "cd api && npm run lint",
    "lint:web": "cd web && npm run lint"
  }
}
