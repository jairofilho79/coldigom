# Database
POSTGRES_USER=praise_user
POSTGRES_PASSWORD=praise_password
POSTGRES_DB=praise_db
POSTGRES_PORT=5432

# Backend API
API_PORT=8000

# Frontend
FRONTEND_PORT=3000
FRONTEND_API_BASE_URL=

# Deployment Configuration
DEPLOYMENT_ENV=local  # local ou vps
DEPLOYMENT_HOST=192.168.1.100  # IP local ou URI do VPS (ex: api.exemplo.com)

# Storage Configuration (modo local para produção beta)
STORAGE_MODE=local
STORAGE_LOCAL_PATH=/storage/assets  # Caminho dentro do container Docker

# Wasabi Storage (desabilitado temporariamente - será usado após fase beta)
# WASABI_ACCESS_KEY=
# WASABI_SECRET_KEY=
# WASABI_ENDPOINT=https://s3.wasabisys.com
# WASABI_BUCKET=
# WASABI_REGION=us-east-1

# JWT
JWT_SECRET_KEY=your-secret-key-here-change-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost
